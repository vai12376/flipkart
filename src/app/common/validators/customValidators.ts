/** @format */

import { AbstractControl, ValidatorFn } from "@angular/forms";

export class CustomValidators {
  // static ImageTypeValidator(control:AbstractControl):ValidatorFn{
  //     return (control)=>{
  //         if()
  //     }
  // }
}
